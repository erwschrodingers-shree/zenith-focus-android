<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zenith.focus">

    <!-- Core Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification Listener Service for DND & Notification Interception -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- Display Over Other Apps for Zen Mode Overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Battery Optimization Exclusion -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Foreground Service for Background Timer -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Boot Receiver for Device Reboot Resilience -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Vibration for Haptic Feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Audio Focus for MP3 Loop Player -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Query Installed Apps for Whitelist Selection -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Screen State for Dim Mode -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Disable Keyguard for Lock Override Prevention -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />

    <!-- Schedule Exact Alarms for Break Timing -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zenith"
        android:usesCleartextTraffic="false"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Zenith">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Zen Mode Lock Activity (No History, Full Screen) -->
        <activity
            android:name=".presentation.ZenModeActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Zenith.FullScreen"
            android:noHistory="true"
            android:excludeFromRecents="true" />

        <!-- Foreground Service: Timer & Lock Manager -->
        <service
            android:name=".service.ZenModeService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Notification Listener Service: DND & Alert Interception -->
        <service
            android:name=".service.ZenthNotificationListenerService"
            android:exported="true"
            android:label="@string/notification_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Boot Completed Receiver -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Alarm Receiver for Break Timing -->
        <receiver
            android:name=".receiver.BreakAlarmReceiver"
            android:exported="true" />

    </application>

</manifest>
